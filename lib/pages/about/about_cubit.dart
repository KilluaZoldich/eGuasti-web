void openRepository() {
    // Updated to point to your GitHub repository
    final uri = Uri.parse("https://github.com/KilluaZoldich/eGuasti-web");
    launchUrl(uri, mode: LaunchMode.externalApplication);
  }